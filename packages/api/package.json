{
  "name": "@diaspo/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured for @diaspo/api yet\""
  },
  "dependencies": {
    "@diaspo/shared": "workspace:*",
    "msw": "^2.6.2",
    "react": "^18.3.1",
    "@tanstack/react-query": "^5.59.0"
  }
}
